<%- include('partials/header'); %>
<body>
    <%- include('partials/navbar'); %>
    <div class="container">
        <div class="card mt-4 rounded shadow">
            <div class="card-header d-flex justify-content-between">
                <h3>Perguntas</h3>
                <a href="/perguntar" class="btn btn-primary">Perguntar</a>
            </div>
        </div>
        <% questions.forEach(question => { %>
            <div class="card mt-4 rounded shadow">
                <div class="card-body">
                    <h4> <%= question.titulo %> </h4>
                </div>
                <div class="card-footer d-flex justify-content-between align-items-center">
                    <a href="/pergunta/<%= question.id %>"><button class="btn btn-primary">Responder</button></a>
                    <p>Há <%= dateFns.formatDistance(question.createdAt, new Date(), {locale: pt})%> </p>
                </div>
            </div>
        <% }) %>
    </div>
</body>
<%- include('partials/footer'); %>